export enum Repository {
  BOOKING = 'bookings',
  USER = 'users',
  ROOM = 'Rooms',
}

export enum Env {
  SECRET = 'secret',
  DB = 'database',
  HOST = 'host',
  USERNAME = 'db_user',
  PASSWORD = 'password',
}

export enum Cookies {
  User = 'usr',
}

export enum CsvConfig {
  path = './csv_data',
}
